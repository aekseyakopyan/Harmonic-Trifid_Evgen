# Active Learning Flow

## 🔄 Цикл непрерывного обучения

Система использует **Active Learning (AL)** для минимизации ручной разметки и повышения качества классификации лидов. 
Вместо того чтобы размечать тысячи случайных сообщений, мы отбираем только те, в которых модели (BERT/ML) "сомневаются".

### 1. Отбор кандидатов (Sampling)
Раз в неделю (или по запросу `/learn`) запускается `ActiveLearningPipeline`.
Он анализирует все неразмеченные лиды за последние 7 дней.

**Алгоритм отбора (Informativeness Strategy):**
Используется комбинация метрик для оценки неопределенности:
- **Least Confidence**: `1 - max(prob)` — насколько модель не уверена в лучшем классе.
- **Margin Sampling**: `prob_lead - prob_spam` — если разница мала, модель колеблется.
- **Entropy**: Высокая энтропия предсказаний означает "кашу" в голове модели.
- **Committee Variance**: Если BERT говорит "Да", а ML-классификатор "Нет" (разногласие моделей), этот пример приоритетен.

Формула: 
```python
Score = 0.25 * LeastConf + 0.35 * Margin + 0.20 * Entropy + 0.20 * Variance
```

### 2. Человеческая валидация (Human Review)
Отобранные **Топ-50** самых "сложных" примеров отправляются администратору в Telegram через Gwen Bot.
Команда: `/review_batch`

Интерфейс:
- Бот показывает текст сообщения.
- Предсказанные вероятности (BERT: 55%, ML: 48%).
- Кнопки: `[✅ Lead]`, `[❌ Spam]`, `[🤷 Skip]`.

### 3. Накопление данных (Dataset Update)
Размеченные примеры сохраняются в базу с флагом `manual_label=True`.
Это "Золотой стандарт" для дообучения.

### 4. Дообучение (Retraining)
Как только накапливается **50+ новых** размеченных примеров:
1. Запускается процесс `retrain_pipeline`.
2. BERT модель (`rubert-tiny-lead`) дообучается на обновленном датасете.
3. Новая модель сохраняется с версией (timestamp).
4. Статистика (F1-score, accuracy) отправляется в лог.

## 📊 Мониторинг
Команда `/review_stats` в Telegram показывает:
- Текущую точность моделей.
- Количество размеченных примеров.
- Очередь на разметку.

## 🛠 Технические компоненты
- **Script**: `systems/parser/active_learner.py`
- **Trigger**: `scripts/trigger_active_learning.py`
- **Bot Handler**: `systems/gwen/handlers/review_commands.py`
- **DB Methods**: `VacancyDatabase.get_unlabeled_leads_since`, `update_lead_label`
