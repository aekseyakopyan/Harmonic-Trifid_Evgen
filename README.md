# 🤖 Harmonic Trifid — AI Sales System (Alexey & Gwen)

**Harmonic Trifid** — это комплексная экосистема для автоматизации B2B-продаж и поиска клиентов в Telegram. Система объединяет в себе парсинг лидов, их многоуровневую фильтрацию, генерацию персонализированных откликов и ведение автоматизированных диалогов.

---

## 🏗 Структура проекта

Проект организован по модульному принципу:

### 1. `core/` (Ядро системы)
*   `ai_engine/`: Клиенты для работы с LLM (DeepSeek, OpenAI, Anthropic) и локальными моделями (Ollama).
*   `config/`: Настройки (`settings.py`), динамические фильтры слов (`dynamic_filters.json`) и промпты.
*   `utils/`: Логирование, работа с БД и вспомогательные функции.

### 2. `systems/` (Функциональные системы)
*   `parser/`: **Центр обработки лидов**. Включает в себя:
    *   `lead_filter_advanced.py`: Основной конвейер фильтрации (5 уровней проверки).
    *   `ml_classifier.py`: ML-модель для классификации лидов (Logistic Regression, точность ~91%).
    *   `duplicate_detector.py`: Защита от дублей на основе нечеткого сравнения текстов за 48 часов.
    *   `entity_extractor.py`: NER-модуль для извлечения бюджетов, дедлайнов и контактов.
    *   `lead_scoring.py`: Расчет приоритета лида (0-100) и категоризация (`HOT`, `WARM`, `COLD`).
    *   `outreach_generator.py`: Генератор персонализированных откликов от лица Алексея.
    *   `workflow.py`: Автоматизация действий (уведомления, моментальная генерация офферов).
*   `alexey/`: Личность и логика ведения диалогов (обработка входящих, RAG, имитация человека).
*   `gwen/`: Командный центр для управления системой через Telegram (`Commander`).
*   `dashboard/`: Серверная часть для веб-панели управления.

### 3. `scripts/` (Служебные скрипты)
*   `train_ml_model.py`: Обучение ML-классификатора на накопленных данных.
*   `filter_analytics.py`: Получение отчетов по качеству фильтрации и источникам лидов.
*   `export_db_to_excel.py`: Выгрузка всей базы `vacancies.db` в Excel.
*   `revalidate_existing_leads.py`: Массовая перепроверка старых лидов новым алгоритмом.

---

## 🚀 Логика обработки лида (Pipeline)

Когда система находит новое сообщение в отслеживаемых каналах, оно проходит через **Advanced Filter Pipeline**:

1.  **Level 0: Нормализация**: Очистка текста, извлечение признаков (эмодзи, капс, ссылки, бюджет).
2.  **Level 1: Hard Blocks**: Мгновенный отсев по блэклисту доменов, ботов, мошеннических паттернов и слишком коротких текстов.
3.  **Level 2: Deduplication**: Проверка на дубликаты. Если похожий запрос уже был в других чатах за последние 48 часов — сообщение отклоняется.
4.  **Level 3: Heuristic Scoring**: Начисление баллов на основе ключевых слов заказчика (+), исполнителя (-) и признаков спама (-).
5.  **Level 4: Context Validation**: Корректировка счета в зависимости от надежности источника и релевантности ниши (SEO, Авито, Сайты).
6.  **Level 5: ML Classifier**: Если счет спорный, подключается обученная модель Logistic Regression.
7.  **Level 6: LLM Deep Analysis**: В самых сложных случаях выполняется глубокий анализ через облачную LLM (DeepSeek).

### 🎯 Результат обработки:
*   **Rejected**: Причина отказа сохраняется в БД для аналитики.
*   **Accepted**: Лид получает:
    *   **Priority (0-100)**: Чем выше бюджет и срочность, тем выше приоритет.
    *   **Tier**: `HOT` (немедленная реакция), `WARM` (ручное ревью), `COLD` (просто сохранение).
    *   **Entities**: Отдельно выделенные контактные данные, суммы и дедлайны.

---

## 📊 Управление и Аналитика

Для контроля системы используются:
1.  **Gwen Commander**: Управление через Telegram-команды (`/status`, `/stats`, `/outreach`).
2.  **Filter Analytics**: Команда `python3 scripts/filter_analytics.py 7` выводит детальный отчет по эффективности за неделю.
3.  **Excel Export**: Актуальный список лидов всегда доступен через выгрузку в Excel.

---

## 🛠 Установка и запуск

1.  **Зависимости**: `pip install -r requirements.txt`. Также требуются `scikit-learn` и `pandas` для ML и экспорта.
2.  **База данных**: Автоматически создается `vacancies.db` при первом запуске.
3.  **Обучение**: После набора первых 100-200 размеченных лидов запустите `python3 scripts/train_ml_model.py`.
4.  **Командный центр**: `python3 systems/gwen/commander.py`.

---
*Документация обновлена: Февраль 2026 (Добавлен Advanced Lead Filter v2)*
